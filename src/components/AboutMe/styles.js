import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  rootContainer: {
    margin: "0",
    padding: "0",
  },
  mainContainer: {},
  grid: {
    height: "10rem",
    display: "flex",
    justifyContent: "center",
  },
}));
