import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  rootContainer: {
    margin: "0",
    padding: "0",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  mainContainer: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
}));
