import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  headerContainer: {},
  headerIcon: {},
  header: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
}));
