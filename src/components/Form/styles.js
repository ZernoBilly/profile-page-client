import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  paper: {
    padding: "30px",
  },
  form: {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "center",
  },
});
