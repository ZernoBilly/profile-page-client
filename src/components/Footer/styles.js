import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  gridContainer: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
  },
});
